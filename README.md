# git_assignment_HeroVired

1. Created a private GitHub repository named `git_assignment_HeroVired`.
2. Created a `dev` branch from `main` and added base calculator code.
3. Merged `dev` into `main` and created **Version 1 (v1.0)** release.
4. Adding a course mate as a collaborator to enable collaboration on a private repositoryis the expectation. Due to last minute assessment work, i didnot perform this, istead kept repository public.
5. Created a feature branch `feature/sqrt` for square root functionality.
6. While working on the feature branch, a critical bug was reported in the `divide` function.
7. Switched back to the `dev` branch and fixed the divide-by-zero issue.
8. Synced the updated `dev` branch into the `feature/sqrt` branch.
9. Raised a pull request from `feature/sqrt` to `main`.
10. Performed a pull request review using comments to validate functionality.
11. Merged the feature branch into `dev` by myself without review and approval from peers.
12. Performed final testing in the `dev` branch.
13. Merged `dev` into `main`.
14. Created **Version 2 (v2.0)** release.
